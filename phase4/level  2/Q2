يمكن إضافة نموذج جديد في Django عن طريق تطبيق الخطوات التالية:

1. تعريف النموذج: في الملف models.py في التطبيق الذي ترغب في إنشاء نموذج جديد، أضف تعريفًا للنموذج الجديد. على سبيل المثال:
  
from django.db import models

class شخص(models.Model):
    الاسم = models.CharField(max_length=255)
    العمر = models.IntegerField()
2. إنشاء ترحيل (Migration): بعد تعريف النموذج الجديد، يجب إنشاء ترحيل جديد لتعكس التغييرات في قاعدة البيانات. استخدم الأمر التالي:
  
python manage.py makemigrations
   هذا الأمر سيدفعك إلى إنشاء ترحيل جديد في المجلد migrations في التطبيق.
3. تطبيق الترحيل: بعد إنشاء الترحيل، يجب تطبيقه على قاعدة البيانات. استخدم الأمر التالي:
  
python manage.py migrate
   هذا الأمر سيدفعك إلى تطبيق الترحيل وتحديث قاعدة البيانات.
4. إجراء الترحيل (Apply Migration): إذا كنت ترغب في إجراء الترحيل يدويًا، يمكنك استخدام الأمر التالي:
  
python manage.py applymigrations
   هذا الأمر سيدفعك إلى تطبيق الترحيل وتحديث قاعدة البيانات.
